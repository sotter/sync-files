#使git ignore支持已经track过的文件
#git ls-files -z 目录 |xargs -0 git update-index --assume-unchanged
# 单个文件: git update-index --assume-unchanged <file name>

.idea/
img/
sync-client/sync-client
sync-server/sync-server
sync-server/sync-files/
ut
*.toml
.DS_Store
